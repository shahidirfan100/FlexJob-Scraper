{
  "title": "FlexJobs Jobs Scraper",
  "description": "Scrapes job listings from FlexJobs by crawling public category and job pages. Simply provide start URLs and set your preferences.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "List of FlexJobs category or listing URLs to start crawling from. The scraper will automatically find more links.",
      "prefill": [
        "https://www.flexjobs.com/remote-jobs",
        "https://www.flexjobs.com/remote-jobs/legitimate-work-from-home-jobs-hiring-now"
      ],
      "editor": "stringList",
      "sectionCaption": "Start URLs"
    },
    "results_wanted": {
      "title": "Maximum Jobs",
      "type": "integer",
      "description": "The maximum number of jobs to collect. Leave blank for unlimited.",
      "minimum": 1,
      "editor": "number",
      "default": 100,
      "sectionCaption": "Data Collection"
    },
    "maxPagesPerList": {
      "title": "Maximum Pages per List",
      "type": "integer",
      "description": "Maximum number of pages to visit per category to prevent excessive crawling.",
      "minimum": 1,
      "editor": "number",
      "default": 25
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "integer",
      "description": "Maximum number of simultaneous requests for faster scraping.",
      "minimum": 1,
      "default": 10,
      "sectionCaption": "Advanced Configuration"
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Use Apify Proxy to avoid blocking. Residential proxies recommended if needed.",
      "editor": "proxy"
    },
    "cookies": {
      "title": "Cookies",
      "type": "array",
      "description": "Optional cookies for requests. Format: [{\"name\": \"sessionid\", \"value\": \"abc123\"}].",
      "editor": "json",
      "sectionCaption": "Optional Settings"
    }
  },
  "required": [],
  "additionalProperties": false
}
